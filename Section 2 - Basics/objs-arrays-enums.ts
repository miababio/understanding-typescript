const person: {
    name: string;
    age: number;
    hobbies: string[],
    role: [number, string];
} = {
// const person = {
    name: 'Michael',
    age: 27,
    hobbies: ['Sports', 'Cooking'],
    role: [2, 'author']
};

// person.role.push('admin');
// person.role[1] = 10;

console.log(person);